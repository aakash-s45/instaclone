import 'package:flutter/material.dart';

class Zoomed extends StatelessWidget {
  const Zoomed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
